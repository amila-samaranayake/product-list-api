{
  "name": "product-list-api",
  "version": "1.0.0",
  "description": "This is a dummy api for products listing",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc --build --clean && tsc --build && node ./dist/src/server.js",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "author": "Amila Samaranayake",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "winston": "^3.8.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.15",
    "typescript": "^4.9.4"
  }
}
